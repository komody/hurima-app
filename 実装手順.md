# 認証機能実装手順

## 使用技術
- Fortify
- FormRequest
- Mailhog

## 実装フェーズ

---

## フェーズ1: 環境準備と基本設定 ✅

### 1. Fortifyのインストールと設定
- [x] `composer require laravel/fortify` でインストール済み
- [x] `php artisan vendor:publish --provider="Laravel\Fortify\FortifyServiceProvider"` で設定ファイル公開済み
- [x] `config/fortify.php` で `emailVerification()` が有効
- [x] `AppServiceProvider` でログイン・登録ビューを設定済み

### 2. Mailhogの設定
- [x] `.env` ファイルに以下を設定：
  ```
  MAIL_MAILER=smtp
  MAIL_HOST=localhost
  MAIL_PORT=1025
  MAIL_USERNAME=null
  MAIL_PASSWORD=null
  MAIL_ENCRYPTION=null
  MAIL_FROM_ADDRESS=noreply@hurima.local
  MAIL_FROM_NAME="${APP_NAME}"
  ```

### 3. マイグレーションの修正
- [x] `postal_code` を `nullable()` に変更
- [x] `address` を `nullable()` に変更
- [x] `first_login_email_verified_at` カラムを追加

### 4. Userモデルの更新
- [x] `MustVerifyEmail` インターフェースを実装済み
- [x] `fillable` に必要なフィールドを追加済み
- [x] `casts` に `first_login_email_verified_at` を追加済み

### 5. プロフィール設定画面の実装
- [x] レイアウト実装済み (`resources/views/mypage/profile/edit.blade.php`)
- [x] スタイル実装済み (`resources/css/mypage/profile/edit.scss`)

---

## フェーズ2: FormRequestとバリデーション

### 1. RegisterRequestの作成

**ファイル**: `app/Http/Requests/RegisterRequest.php`

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class RegisterRequest extends FormRequest
{
    public function authorize()
    {
        return true;
    }

    public function rules()
    {
        return [
            'name' => 'required|string|max:255',
            'email' => 'required|string|email|max:255|unique:users',
            'password' => 'required|string|min:8|confirmed',
        ];
    }

    public function messages()
    {
        return [
            'name.required' => 'お名前を入力してください',
            'email.required' => 'メールアドレスを入力してください',
            'email.email' => 'メールアドレスはメール形式で入力してください',
            'password.required' => 'パスワードを入力してください',
            'password.min' => 'パスワードは8文字以上で入力してください',
            'password.confirmed' => 'パスワードと一致しません',
        ];
    }
}
```

### 2. LoginRequestの作成

**ファイル**: `app/Http/Requests/LoginRequest.php`

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class LoginRequest extends FormRequest
{
    public function authorize()
    {
        return true;
    }

    public function rules()
    {
        return [
            'email' => 'required|string|email|max:255',
            'password' => 'required|string',
        ];
    }

    public function messages()
    {
        return [
            'email.required' => 'メールアドレスを入力してください',
            'email.email' => 'メールアドレスはメール形式で入力してください',
            'password.required' => 'パスワードを入力してください',
        ];
    }
}
```

### 3. 日本語バリデーションメッセージの設定

**ファイル**: `resources/lang/ja/validation.php` を作成

または、`config/app.php` の `locale` を `ja` に設定し、日本語のバリデーションファイルを追加

---

## フェーズ3: 会員登録機能

### 1. CreateNewUserアクションの修正

**ファイル**: `app/Actions/Fortify/CreateNewUser.php`

- `RegisterRequest` を使用するように修正
- 登録時に `postal_code` と `address` を `null` で作成
- 登録後にメール認証誘導画面へリダイレクト

### 2. 登録フォームの修正

**ファイル**: `resources/views/auth/register.blade.php`

- フォームの `action` と `method` を設定
- CSRFトークンを追加
- エラーメッセージの表示を追加
- `@error` ディレクティブでエラー表示

### 3. 登録後のリダイレクト処理

- 登録成功後、メール認証誘導画面 (`auth.verify-email`) へリダイレクト
- `FortifyServiceProvider` または `CreateNewUser` で処理

---

## フェーズ4: ログイン機能

### 1. ログインフォームの修正

**ファイル**: `resources/views/auth/login.blade.php`

- フォームの `action` と `method` を設定
- CSRFトークンを追加
- エラーメッセージの表示を追加

### 2. ログイン処理のカスタマイズ

**ファイル**: `app/Providers/FortifyServiceProvider.php` またはカスタムログイン処理

- ログイン成功後のリダイレクト先を設定
- メール認証未完了の場合はメール認証誘導画面へ
- プロフィール未完了（`postal_code` または `address` が `null`）の場合はプロフィール設定画面へ

### 3. 未認証ユーザーのリダイレクト

**ファイル**: `app/Http/Middleware/Authenticate.php`

- 認証が必要なページにアクセスした場合、ログイン画面へリダイレクト

---

## フェーズ5: メール認証機能

### 1. メール認証誘導画面の実装

**ファイル**: `resources/views/auth/verify-email.blade.php`

- 「認証はこちらから」ボタンのリンクを設定
- 「認証メールを再送する」リンクの実装

### 2. メール認証ルートの設定

**ファイル**: `routes/web.php`

```php
// メール認証誘導画面
Route::get('/email/verify', function () {
    return view('auth.verify-email');
})->middleware('auth')->name('verification.notice');

// メール認証処理
Route::get('/email/verify/{id}/{hash}', function (EmailVerificationRequest $request) {
    $request->fulfill();
    
    // プロフィール未完了の場合はプロフィール設定画面へ
    if (is_null($request->user()->postal_code) || is_null($request->user()->address)) {
        return redirect()->route('mypage.profile.edit');
    }
    
    return redirect()->route('items.index');
})->middleware(['auth', 'signed'])->name('verification.verify');

// 認証メール再送
Route::post('/email/verification-notification', function (Request $request) {
    $request->user()->sendEmailVerificationNotification();
    return back()->with('message', '認証メールを送信しました');
})->middleware(['auth', 'throttle:6,1'])->name('verification.send');
```

### 3. 初回ログイン時のメール認証処理

- ログイン時に `first_login_email_verified_at` が `null` かチェック
- `null` の場合はメール認証を送信し、メール認証誘導画面へリダイレクト
- 認証完了時に `first_login_email_verified_at` を更新

### 4. カスタムメール認証Notification

**ファイル**: `app/Notifications/VerifyEmail.php` (必要に応じて)

- 会員登録時と初回ログイン時で異なるメールを送信する場合に作成

---

## フェーズ6: プロフィール設定画面への遷移

### 1. プロフィール未完了チェックのミドルウェア作成

**ファイル**: `app/Http/Middleware/EnsureProfileCompleted.php`

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;

class EnsureProfileCompleted
{
    public function handle(Request $request, Closure $next)
    {
        if (Auth::check()) {
            $user = Auth::user();
            
            // postal_codeまたはaddressがnullの場合はプロフィール設定画面にリダイレクト
            if (is_null($user->postal_code) || is_null($user->address)) {
                // プロフィール設定画面へのアクセスは許可（無限ループを防ぐ）
                if ($request->routeIs('mypage.profile.*')) {
                    return $next($request);
                }
                
                return redirect()->route('mypage.profile.edit')
                    ->with('message', 'プロフィール情報を入力してください');
            }
        }

        return $next($request);
    }
}
```

### 2. Kernel.phpにミドルウェアを登録

**ファイル**: `app/Http/Kernel.php`

```php
protected $routeMiddleware = [
    // ... 既存のミドルウェア ...
    'profile.completed' => \App\Http\Middleware\EnsureProfileCompleted::class,
];
```

### 3. ルートにミドルウェアを適用

**ファイル**: `routes/web.php`

```php
// プロフィール設定画面以外の認証が必要なルートに適用
Route::middleware(['auth', 'profile.completed'])->group(function () {
    Route::get('/sell', [SellController::class, 'create'])->name('sell.create');
    Route::get('/purchase/{item_id}', [PurchaseController::class, 'show'])->name('purchase.show');
    // ... その他の認証が必要なルート
});

// プロフィール設定画面は認証のみ（profile.completedは適用しない）
Route::middleware('auth')->group(function () {
    Route::get('/mypage/profile', [MypageController::class, 'editProfile'])->name('mypage.profile.edit');
    Route::put('/mypage/profile', [MypageController::class, 'updateProfile'])->name('mypage.profile.update');
});
```

### 4. プロフィール設定画面の機能実装

**ファイル**: `app/Http/Controllers/MypageController.php`

- `updateProfile` メソッドの実装
- バリデーション
- 画像アップロード処理
- 更新後のリダイレクト（商品一覧画面へ）

### 5. プロフィール設定フォームの修正

**ファイル**: `resources/views/mypage/profile/edit.blade.php`

- フォームの `action` と `method` を設定
- CSRFトークンと `@method('PUT')` を追加
- エラーメッセージの表示を追加
- 既存のプロフィール情報を表示

---

## 仕様確認事項

### エラーメッセージ（仕様書より）

#### 会員登録時
- 未入力の場合：
  - お名前を入力してください
  - メールアドレスを入力してください
  - メールアドレスはメール形式で入力してください
  - パスワードを入力してください
- パスワードの入力規則違反：
  - パスワードは8文字以上で入力してください
- 確認用パスワードの入力規則違反：
  - パスワードと一致しません

#### ログイン時
- 未入力の場合：
  - メールアドレスを入力してください
  - パスワードを入力してください
- 入力情報が誤っている場合：
  - ログイン情報が登録されていません

### 画面遷移フロー

1. **会員登録時**
   - 会員登録画面 → メール認証誘導画面 → メール認証画面 → プロフィール設定画面 → 商品一覧画面

2. **初回ログイン時**
   - ログイン画面 → メール認証誘導画面 → メール認証画面 → プロフィール設定画面 → 商品一覧画面

3. **通常ログイン時**
   - ログイン画面 → 商品一覧画面（プロフィール完了済みの場合）

---

## 実装チェックリスト

### フェーズ1 ✅
- [x] Fortifyのインストール
- [x] Mailhogの設定
- [x] マイグレーションの修正
- [x] Userモデルの更新
- [x] プロフィール設定画面のレイアウト

### フェーズ2
- [ ] RegisterRequestの作成
- [ ] LoginRequestの作成
- [ ] 日本語バリデーションメッセージの設定

### フェーズ3
- [ ] CreateNewUserアクションの修正
- [ ] 登録フォームの修正
- [ ] 登録後のリダイレクト処理

### フェーズ4
- [ ] ログインフォームの修正
- [ ] ログイン処理のカスタマイズ
- [ ] 未認証ユーザーのリダイレクト

### フェーズ5
- [ ] メール認証誘導画面の実装
- [ ] メール認証ルートの設定
- [ ] 初回ログイン時のメール認証処理
- [ ] 認証メール再送機能

### フェーズ6
- [ ] プロフィール未完了チェックのミドルウェア作成
- [ ] ルートにミドルウェアを適用
- [ ] プロフィール設定画面の機能実装
- [ ] プロフィール設定フォームの修正

---

## 注意事項

1. **メール認証のタイミング**
   - 会員登録時：`email_verified_at` を更新
   - 初回ログイン時：`first_login_email_verified_at` を更新

2. **プロフィール設定の強制**
   - `postal_code` または `address` が `null` の場合は、プロフィール設定画面へのアクセスを強制

3. **認証後の遷移先**
   - プロフィール設定完了後は商品一覧画面 (`items.index`) へ遷移

4. **エラーメッセージ**
   - 仕様書に記載されている文言を厳密に守る（評価項目）
